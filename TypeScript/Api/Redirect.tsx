/* * * * * * * | Auto Generated Script, do not edit | * * * * * * * */
// Imports
import {VersionedContent} from './Content'
import {AutoController} from './ApiEndpoints'
import {RedirectIncludes, ApiIncludes} from './Includes'
// eslint-disable-next-line no-restricted-imports
import {getOne, getList, getJson, getText} from 'UI/Functions/WebRequest'

// Module
/*
  A Redirect
*/
export type Redirect = VersionedContent<uint> & {
    from?: string,
    to?: string,
    permanentRedirect: boolean,
}

/**
*/
export class RedirectApi extends AutoController<Redirect, uint>{
    /**
      AutoGenerated, ensures all default CRUD functionality

    */
    public constructor (){
        super('redirect')
        this.includes = new RedirectIncludes('', '');
    }

}

export default new RedirectApi();
